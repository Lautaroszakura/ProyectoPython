# ProyectoPython

Alumno: Szakura Lautaro Ivan

#Para crear una aplicacion, permite separar el codigo en partes y reutilizarla en otro proyecto

    python manage.py startapp myapp

#Se la debe registrar en Settings Dentro de la carpeta principal

#Para crear el template, Dentro de myapp, se crea una carpeta Templates y luego otra carpeta con el nombre de la aplicaion

    ProyectoPython/myapp/templates/myapp
    Se crea un archivo index.html dentro de myapp

#En myapp/views.py, se creara una funcion

#Se debe crear un urls de la aplicacion general